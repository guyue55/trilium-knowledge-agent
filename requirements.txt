--extra-index-url https://download.pytorch.org/whl/cu121

# （Front）Web framework
streamlit>=1.22.0

# Logging
loguru

# Core web framework
fastapi~=0.95.0
uvicorn==0.21.1
pydantic==1.10.7

# Database and vector storage
chromadb==0.4.24
onnxruntime==1.17.0
pulsar-client==3.8.0

# GPU Acceleration (PyTorch + CUDA 12.1)
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# AI and language processing (使用最新稳定版)
langchain==0.1.16              # 核心框架
langchain-community==0.0.34    # 社区集成
langchain-core~=0.1.42         # 核心组件
langsmith~=0.1.20              # 监控和调试
dashscope~=1.14.1              # 阿里云千问API

# Frontend and utilities
streamlit==1.33.0
requests==2.32.3
watchdog==3.0.0
python-dotenv==1.0.0

# Language models
gpt4all==2.2.1                 # 包含性能优化

# HuggingFace components & Acceleration
huggingface-hub>=0.36.0        # 解决版本兼容性问题
sentence-transformers>=3.0.0   # 解决导入错误 (cached_download)
transformers>=4.40.0           # 核心模型库
accelerate>=0.21.0             # GPU加速推理
bitsandbytes>=0.41.0           # 模型量化(8bit/4bit)

# Trilium integration
trilium-py==1.3.0

# Analysis and visualization
jieba~=0.42.1
Jinja2~=3.1
